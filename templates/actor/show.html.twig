{% extends 'base.html.twig' %}

{% block body %}
    <div class="media">
        <div class="media-body">
            <h1 class="mt-0">Acteur de merde dont tout le monde se fout : {{ actor.name }}</h1>
            <h3>Liste des programes avec l'autre con : </h3>
            <ul>
                {% for program in actor.programs %}
                    <li><a href="{{ path('program_show', {id: program.id}) }}"{{ program.title }}>Voir sa série pétée</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>

{% endblock %}